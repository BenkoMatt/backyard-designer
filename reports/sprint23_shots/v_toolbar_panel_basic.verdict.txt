**Review of 1280×800 screenshot — issues found (not CLEAN):**

**1) Overlapping / clipped**
- **Compass overlaps "Take Tour" button (top-right):** the white compass circle's left edge sits on top of the green button's right end — sloppy collision between two always-visible chrome elements. Nudge the compass right/down or the tour button left.
- No other overlaps detected: the Terrain Sculpting panel (443–750 × 343–680) clears the bottom toolbar (~694+) and status bar, sidebar clears the panel, zoom controls and scale bar fine.

**2) 5-second comprehension — mostly yes, with caveats**
- Good: the sculpting panel auto-opens with a plain-language hint ("Pick a mode, then click + drag on the ground to sculpt"), "Raise" is pre-selected, and there's a "Take Tour" affordance. Core interaction is discoverable.
- Hurts it: the bottom toolbar offers five similarly weighted buttons (Tape Measure / Terrain / Excavate / Analyze / **Innovate**) plus a *second* green "Terrain" button bottom-left — a new user can't tell which "Terrain" does what. "Innovate" in particular is meaningless without a tooltip.

**3) Confusing / ambiguous / broken-looking**
- **"FPS: 1" in the status bar** — reads as a broken render or severe performance problem; either fix the perf or the metric. As shown, the app looks like it would be unusable to drag-sculpt.
- **Two competing "Terrain" controls** (green panel tab, bottom-left vs. brown active tool button, bottom-center) — ambiguous duplication.
- **"Strength: 0.05"** — no unit/scale (%? multiplier?); unclear whether higher is stronger.
- **"Height at cursor: 0.0 ft"** shown while Precision Mode is Off — looks like a dead/disabled readout with no explanation of how to enable it.
- Minor: the floating "Daytime" pill (top center) gives no affordance that it's a day/night toggle.