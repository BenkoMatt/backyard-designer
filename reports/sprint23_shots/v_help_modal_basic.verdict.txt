**Mostly clean — minor notes only:**

1. **Overlaps/clipping:** All occlusion is from the help modal covering the canvas, bottom toolbar (brush slider, "10 ft", "Sun" control), and the "Daytime" chip — this is expected modal behavior, not a defect. Nothing inside the modal itself is clipped or colliding. No true bugs found.

2. **5-second comprehension:** Yes. The onboarding modal immediately states the core loop (click item in left panel → click/select in 3D view → drag → adjust in right panel), and the left panel repeats "Click any item below to add it to your yard." A new user would know what to do.

3. **Minor ambiguities (not blockers):**
   - The instructions say to "use the **right panel** to adjust size, color," but no right-side properties panel is visible anywhere — a new user following that step may look for something that isn't on screen yet (presumably it appears on selection).
   - The modal's close (X) button sits inline next to the "Basic" badge rather than at the modal's top-right corner, which reads slightly oddly.
   - The "Basic" badge on the modal title duplicates the active "Basic" tab in the top bar — redundant but harmless.

If those two微 nits are acceptable, this is effectively **CLEAN**.